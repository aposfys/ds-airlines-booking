# Pinned for reproducible builds. Resolved 2026-07-29 against Python 3.13.
# Phase 1 moves this to pyproject.toml with a proper lockfile.

# ── Web framework ─────────────────────────────────────────
fastapi==0.141.1
uvicorn[standard]==0.52.0
python-multipart==0.0.32

# ── Data & validation ─────────────────────────────────────
pydantic==2.13.4
email-validator==2.3.0

# ── Database ──────────────────────────────────────────────
# PostgreSQL from Phase 1 — docs/adr/0001-postgresql-over-mongodb.md
sqlalchemy[asyncio]==2.0.51
asyncpg==0.31.0
alembic==1.18.5

# ── Auth ──────────────────────────────────────────────────
# passlib omitted deliberately: it is unmaintained and its bcrypt
# backend breaks against bcrypt>=4. We call bcrypt directly.
python-jose[cryptography]==3.5.0
bcrypt==5.0.0

# ── Outbound HTTP ─────────────────────────────────────────
# Runtime, not just tests: the weather proxy calls Open-Meteo server-side so
# the browser never talks to a third party directly.
httpx==0.28.1

# ── Config ────────────────────────────────────────────────
python-dotenv==1.2.2

# ── Testing ───────────────────────────────────────────────
pytest==9.1.1
pytest-asyncio==1.4.0
